import React from 'react';
import Header from './Header';

const Manif = () => {
    window.scrollTo(0, 0);
    return (
     <>
     <Header/>

    <div>A finir de coder</div>
     </>
    );
};

export default Manif;